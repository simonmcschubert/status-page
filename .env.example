# Database
DATABASE_URL=postgresql://statuspage:password@localhost:5432/statuspage

# Redis
REDIS_URL=redis://localhost:6379

# Server
PORT=3000
NODE_ENV=development

# Admin Setup (only needed on first run)
# Set these to auto-create an admin user on startup
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=changeme123

# Optional: Set to 'true' to reset admin password on next restart
# ADMIN_PASSWORD_RESET=true

# JWT Secrets (generate with: openssl rand -base64 32)
JWT_SECRET=your-jwt-secret-change-in-production
